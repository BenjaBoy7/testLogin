import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; 
import firebase from './firebaseConfig'; // Import firebaseConfig.js before using Firebase in other components

ReactDOM.render(<App />, document.getElementById('root'));
